/**
 * 1 - Función boooleana que comprueba
 *     que un valor puede ser tratado como número
 * 
 * 2 - Función que comprueba que es un número válido
 *     para poder calcular un factorial
 * 
 * 3 - Función que calcula el factorial de un número
 *     En caso de valores no válido, genera un error
 * 
 * 4 - Función que muestra (renderiza) los resultados por pantalla
 * 
 * Al menos para las 2 primeras, realzar test unitarios
 * 
 */


let = null 



function inValidNumber (n) { 
	for (i=1; i<=n; i++) {
		total = total * i; 
	}
	return total; 
}

function isEntero (n) {
	let r = true
	let n = 22
	n
	return
}